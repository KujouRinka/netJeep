#include "proxy_interface.h"

ssize_t ConnHandler::parseInRead(conn_p conn, conn_p holder) {
    return 0;
}

ssize_t ConnHandler::parseInWrite(conn_p conn, conn_p holder) {
    return 0;
}

ssize_t ConnHandler::parseOutRead(conn_p conn, conn_p holder) {
    return 0;
}

ssize_t ConnHandler::parseOutWrite(conn_p conn, conn_p holder) {
    return 0;
}